import { GrantedAccessPipePipe } from './granted-access-pipe.pipe';

describe('GrantedAccessPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GrantedAccessPipePipe();
    expect(pipe).toBeTruthy();
  });
});
